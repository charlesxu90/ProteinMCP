mcps:
  LigandMPNN_mcp:
    dependencies: []
    description: '> AI-powered protein design toolkit for scaffold-based sequence
      generation and ligand-aware protein ...'
    env_vars: {}
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/ligandmpnn_mcp
    runtime: python
    server_args:
    - src/server.py
    server_command: python
    setup_commands:
    - '(command -v mamba >/dev/null 2>&1 && mamba create -p ./env python=3.11 -y)
      || (command -v conda >/dev/null 2>&1 && conda create -p ./env python=3.11 -y)
      || (echo "Warning: Neither mamba nor conda found, creating venv instead" &&
      python3 -m venv ./env)'
    - ./env/bin/pip install -r repo/LigandMPNN/requirements.txt
    - ./env/bin/pip install --ignore-installed fastmcp loguru
    source: Tool
    url: https://github.com/dauparas/LigandMPNN
  ProteinMPNN_mcp:
    dependencies: []
    description: '> Model Context Protocol (MCP) server providing access to ProteinMPNN
      protein design capabilities th...'
    env_vars: {}
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/proteinmpnn_mcp
    runtime: python
    server_args:
    - src/server.py
    server_command: python
    setup_commands:
    - '(command -v mamba >/dev/null 2>&1 && mamba create -p ./env python=3.10 -y)
      || (command -v conda >/dev/null 2>&1 && conda create -p ./env python=3.10 -y)
      || (echo "Warning: Neither mamba nor conda found, creating venv instead" &&
      python3 -m venv ./env)'
    - ./env/bin/pip install torch torchvision numpy
    - ./env/bin/pip install loguru click pandas tqdm
    - ./env/bin/pip install --ignore-installed fastmcp
    source: Tool
    url: https://github.com/dauparas/ProteinMPNN
  RFdiffusion2_mcp:
    dependencies: []
    description: '> Protein design and structure prediction tools using RFdiffusion2
      and Chai1, available as MCP tools...'
    env_vars: {}
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/rfdiffusion2_mcp
    runtime: python
    server_args:
    - src/server.py
    server_command: python
    setup_commands:
    - '(command -v mamba >/dev/null 2>&1 && mamba create -p ./env python=3.11 -y)
      || (command -v conda >/dev/null 2>&1 && conda create -p ./env python=3.11 -y)
      || (echo "Warning: Neither mamba nor conda found, creating venv instead" &&
      python3 -m venv ./env)'
    - ./env/bin/pip install fastmcp==2.14.1 loguru==0.7.3 numpy pandas tqdm
    - ./env/bin/pip install chai-lab
    - ./env/bin/pip install --ignore-installed fastmcp
    source: Tool
    url: https://github.com/RosettaCommons/RFdiffusion2
  alphafold3_mcp:
    dependencies: []
    description: '> AI-powered protein structure prediction and variant analysis using
      AlphaFold3 through Model Contex...'
    env_vars: {}
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/alphafold3_mcp
    runtime: python
    server_args:
    - src/server.py
    server_command: python
    setup_commands:
    - '(command -v mamba >/dev/null 2>&1 && mamba create -p ./env python=3.11 -y)
      || (command -v conda >/dev/null 2>&1 && conda create -p ./env python=3.11 -y)
      || (echo "Warning: Neither mamba nor conda found, creating venv instead" &&
      python3 -m venv ./env)'
    - ./env/bin/pip install -r requirements.txt
    - ./env/bin/pip install fastmcp loguru --ignore-installed
    source: Tool
    url: ''
  alphafold_mcp:
    dependencies: []
    description: '> AlphaFold2 protein structure prediction via Model Context Protocol
      (MCP)'
    env_vars: {}
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/alphafold2_mcp
    runtime: python
    server_args:
    - src/server.py
    server_command: python
    setup_commands:
    - '(command -v mamba >/dev/null 2>&1 && mamba create -p ./env python=3.10 -y)
      || (command -v conda >/dev/null 2>&1 && conda create -p ./env python=3.10 -y)
      || (echo "Warning: Neither mamba nor conda found, creating venv instead" &&
      python3 -m venv ./env)'
    - ./env/bin/pip install -r repo/alphafold/requirements.txt
    - ./env/bin/pip install loguru --ignore-installed
    - ./env/bin/pip install --ignore-installed fastmcp
    source: Tool
    url: https://github.com/google-deepmind/alphafold
  boltz_mcp:
    dependencies: []
    description: '> Protein structure and affinity prediction using Boltz2 deep learning
      models via MCP (Model Context...'
    env_vars: {}
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/boltz_mcp
    runtime: python
    server_args:
    - src/server.py
    server_command: python
    setup_commands:
    - '(command -v mamba >/dev/null 2>&1 && mamba create -p ./env python=3.10 -y)
      || (command -v conda >/dev/null 2>&1 && conda create -p ./env python=3.10 -y)
      || (echo "Warning: Neither mamba nor conda found, creating venv instead" &&
      python3 -m venv ./env)'
    - cd repo/boltz && ../../env/bin/pip install -e . && cd ../..
    - ./env/bin/pip install --ignore-installed fastmcp loguru
    source: Tool
    url: https://github.com/jwohlwend/boltz
  boltzgen_mcp:
    dependencies: []
    description: '> AI-powered protein design through the Model Context Protocol -
      Design protein binders, peptide bin...'
    env_vars: {}
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/boltzgen_mcp
    runtime: python
    server_args:
    - src/server.py
    server_command: python
    setup_commands:
    - '(command -v mamba >/dev/null 2>&1 && mamba create -p ./env python=3.12 -y)
      || (command -v conda >/dev/null 2>&1 && conda create -p ./env python=3.12 -y)
      || (echo "Warning: Neither mamba nor conda found, creating venv instead" &&
      python3 -m venv ./env)'
    - ./env/bin/pip install fastmcp==2.13.3 loguru==0.7.3
    - ./env/bin/pip install boltzgen
    - ./env/bin/pip install --ignore-installed fastmcp
    source: Tool
    url: ''
  chai-lab_mcp:
    dependencies: []
    description: '> Protein structure prediction using Chai-1 model via Model Context
      Protocol (MCP)'
    env_vars: {}
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/chai1_mcp
    runtime: python
    server_args:
    - src/server.py
    server_command: python
    setup_commands:
    - '(command -v mamba >/dev/null 2>&1 && mamba create -p ./env python=3.10 -y)
      || (command -v conda >/dev/null 2>&1 && conda create -p ./env python=3.10 -y)
      || (echo "Warning: Neither mamba nor conda found, creating venv instead" &&
      python3 -m venv ./env)'
    - ./env/bin/pip install loguru click pandas numpy tqdm
    - ./env/bin/pip install --force-reinstall --no-cache-dir fastmcp
    - ./env/bin/pip install chai_lab==0.6.1
    source: Tool
    url: https://github.com/chaidiscovery/chai-lab
  esm_mcp:
    dependencies: []
    description: '> **Protein sequence analysis using ESM-2 language models via Model
      Context Protocol (MCP)**'
    env_vars: {}
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/esmfold_mcp
    runtime: python
    server_args:
    - src/server.py
    server_command: python
    setup_commands:
    - '(command -v mamba >/dev/null 2>&1 && mamba create -p ./env python=3.10 pip
      -y) || (command -v conda >/dev/null 2>&1 && conda create -p ./env python=3.10
      pip -y) || (echo "Warning: Neither mamba nor conda found, creating venv instead"
      && python3 -m venv ./env)'
    - ./env/bin/pip install fastmcp loguru
    - '(command -v mamba >/dev/null 2>&1 && mamba env create -f repo/esm/environment.yml
      -p ./env_esmfold) || (command -v conda >/dev/null 2>&1 && conda env create -f
      repo/esm/environment.yml -p ./env_esmfold) || (echo "Warning: Neither mamba
      nor conda found, ESM environment creation failed" && exit 1)'
    - ./env_esmfold/bin/pip install -e repo/esm/
    - ./env/bin/pip install --ignore-installed fastmcp
    source: Tool
    url: https://github.com/facebookresearch/esm
  ev_onehot_mcp:
    dependencies: []
    description: Protein fitness prediction combining evolutionary coupling and one-hot
      encoding
    env_vars: {}
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/ev_onehot_mcp
    runtime: python
    server_args:
    - src/ev_onehot_mcp.py
    server_command: python
    setup_commands:
    - '(command -v mamba >/dev/null 2>&1 && mamba env create -p ./env python=3.10
      -y) || (command -v conda >/dev/null 2>&1 && conda env create -p ./env python=3.10
      -y) || (echo "Warning: Neither mamba nor conda found, creating venv instead"
      && python3 -m venv ./env)'
    - ./env/bin/pip install -r requirements.txt
    - ./env/bin/pip install --ignore-installed fastmcp
    source: Tool
    url: https://github.com/Biomolecular-Design-Nexus/ev_onehot_mcp
  gromacs-2025.4_mcp:
    dependencies: []
    description: '> MCP server for GROMACS 2025.4 providing molecular dynamics simulation,
      analysis, and workflow mana...'
    env_vars: {}
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/gromacs_mcp
    runtime: python
    server_args:
    - src/server.py
    server_command: python
    setup_commands:
    - '(command -v mamba >/dev/null 2>&1 && mamba create -p ./env python=3.10 -y)
      || (command -v conda >/dev/null 2>&1 && conda create -p ./env python=3.10 -y)
      || (echo "Warning: Neither mamba nor conda found, creating venv instead" &&
      python3 -m venv ./env)'
    - ./env/bin/python -m pip install --upgrade pip
    - '(command -v mamba >/dev/null 2>&1 && mamba install -c conda-forge -p ./env
      gromacs=2025.4 -y) || (command -v conda >/dev/null 2>&1 && conda install -c
      conda-forge -p ./env gromacs=2025.4 -y) || echo "Warning: Could not install
      GROMACS via conda/mamba. Please install manually."'
    - ./env/bin/pip install fastmcp loguru --ignore-installed
    source: Tool
    url: ''
  main_mcp:
    dependencies: []
    description: '> Model Control Protocol (MCP) server for Rosetta molecular modeling
      and protein design suite'
    env_vars: {}
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/rosetta_mcp
    runtime: python
    server_args:
    - src/server.py
    server_command: python
    setup_commands:
    - '(command -v mamba >/dev/null 2>&1 && mamba create -p ./env python=3.10 -y)
      || (command -v conda >/dev/null 2>&1 && conda create -p ./env python=3.10 -y)
      || (echo "Warning: Neither mamba nor conda found, creating venv instead" &&
      python3 -m venv ./env)'
    - ./env/bin/pip install numpy pandas loguru click tqdm
    - ./env/bin/pip install --force-reinstall --no-cache-dir fastmcp
    - ./env/bin/pip install biopython matplotlib
    - ./env/bin/pip install --ignore-installed fastmcp
    source: Tool
    url: ''
  mmseqs2_mcp:
    dependencies: []
    description: Sequence search, clustering, and MSA generation using MMseqs2
    env_vars:
      MMSEQS2_DB_PATH: /mnt/data/data_repository/bio-seq/protein/uniref100/uniref100.fasta.db_padded
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/mmseqs2_mcp
    runtime: python
    server_args:
    - src/mmseqs2_mcp.py
    server_command: python
    setup_commands:
    - '(command -v mamba >/dev/null 2>&1 && mamba env create -p ./env python=3.10
      -y && ./env/bin/pip install fastmcp) || (command -v conda >/dev/null 2>&1 &&
      conda env create -p ./env python=3.10 -y && ./env/bin/pip install fastmcp) ||
      (echo "Warning: Neither mamba nor conda found, creating venv instead" && python3
      -m venv ./env && ./env/bin/pip install fastmcp)'
    - 'if command -v wget >/dev/null 2>&1; then wget -q https://mmseqs.com/latest/mmseqs-linux-gpu.tar.gz
      && tar xzf mmseqs-linux-gpu.tar.gz && cp mmseqs/bin/mmseqs ./env/bin/ && rm
      -rf mmseqs mmseqs-linux-gpu.tar.gz; else echo "Warning: wget not found, skipping
      mmseqs2 GPU installation. Install manually or install wget."; fi'
    source: Tool
    url: https://github.com/Biomolecular-Design-Nexus/mmseqs2_mcp
  msa_mcp:
    dependencies: []
    description: Generate Multiple Sequence Alignments (MSA) using ColabFold server
    env_vars: {}
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/msa_mcp
    runtime: python
    server_args:
    - src/msa_mcp.py
    server_command: python
    setup_commands:
    - pip install fastmcp requests
    source: Tool
    url: https://github.com/Biomolecular-Design-Nexus/msa_mcp
  mutcompute_mcp:
    dependencies: []
    description: '> A Model Context Protocol (MCP) server for protein mutation effect
      prediction using neural networks'
    env_vars: {}
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/mutcompute_mcp
    runtime: python
    server_args:
    - src/server.py
    server_command: python
    setup_commands:
    - '(command -v mamba >/dev/null 2>&1 && mamba create -p ./env python=3.10 pip
      -y) || (command -v conda >/dev/null 2>&1 && conda create -p ./env python=3.10
      pip -y) || (echo "Warning: Neither mamba nor conda found, creating venv instead"
      && python3 -m venv ./env)'
    - ./env/bin/pip install fastmcp loguru pandas numpy matplotlib seaborn scipy tqdm
    - ./env/bin/pip install --ignore-installed fastmcp
    - '(command -v mamba >/dev/null 2>&1 && mamba env create -f repo/mutcompute/environment.yaml
      -p ./env_py36 -y) || (command -v conda >/dev/null 2>&1 && conda env create -f
      repo/mutcompute/environment.yaml -p ./env_py36 -y) || (echo "Warning: Legacy
      environment creation requires conda/mamba for neural network support")'
    source: Tool
    url: ''
  netMHCIIpan-4.3_mcp:
    dependencies: []
    description: '> MCP Server for NetMHCIIpan-4.3 - MHC Class II binding prediction
      tools for epitope analysis and im...'
    env_vars: {}
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/netmhc2pan_mcp
    runtime: python
    server_args:
    - src/server.py
    server_command: python
    setup_commands:
    - '(command -v mamba >/dev/null 2>&1 && mamba create -p ./env python=3.10 -y)
      || (command -v conda >/dev/null 2>&1 && conda create -p ./env python=3.10 -y)
      || (echo "Warning: Neither mamba nor conda found, creating venv instead" &&
      python3 -m venv ./env)'
    - ./env/bin/pip install fastmcp loguru click pandas numpy tqdm --no-cache-dir
    - ./env/bin/pip install openpyxl
    - ./env/bin/pip install --ignore-installed fastmcp
    source: Tool
    url: ''
  netMHCpan-4.2_mcp:
    dependencies: []
    description: '> Model Context Protocol (MCP) server for NetMHCpan-4.2, enabling
      AI-powered MHC Class I binding pre...'
    env_vars: {}
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/netmhcpan_mcp
    runtime: python
    server_args:
    - src/server.py
    server_command: python
    setup_commands:
    - '(command -v mamba >/dev/null 2>&1 && mamba create -p ./env python=3.10 -y)
      || (command -v conda >/dev/null 2>&1 && conda create -p ./env python=3.10 -y)
      || (echo "Warning: Neither mamba nor conda found, creating venv instead" &&
      python3 -m venv ./env)'
    - ./env/bin/pip install fastmcp==2.14.1 loguru==0.7.3 pandas==2.3.3 numpy==2.4.0
      tqdm==4.67.1 click==8.3.1
    - ./env/bin/pip install --ignore-installed mcp pydantic uvicorn websockets rich
    source: Tool
    url: ''
  plmc_mcp:
    dependencies: []
    description: Evolutionary coupling analysis using PLMC for protein structure prediction
    env_vars: {}
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/plmc_mcp
    runtime: python
    server_args:
    - src/plmc_mcp.py
    server_command: python
    setup_commands:
    - '(command -v mamba >/dev/null 2>&1 && mamba env create -p ./env python=3.10
      -y && ./env/bin/pip install pydantic fastmcp) || (command -v conda >/dev/null
      2>&1 && conda env create -p ./env python=3.10 -y && ./env/bin/pip install pydantic
      fastmcp) || (echo "Warning: Neither mamba nor conda found, creating venv instead"
      && python3 -m venv ./env && ./env/bin/pip install pydantic fastmcp)'
    - ./env/bin/pip install https://github.com/debbiemarkslab/EVcouplings/archive/develop.zip
    - if [ ! -d "./repo/plmc" ]; then mkdir -p ./repo && cd ./repo && git clone https://github.com/debbiemarkslab/plmc.git
      && cd plmc && make all-openmp; else echo "plmc already exists, skipping clone
      and build"; fi
    - '(command -v mamba >/dev/null 2>&1 && mamba install -c conda-forge -c bioconda
      hhsuite -y) || (command -v conda >/dev/null 2>&1 && conda install -c conda-forge
      -c bioconda hhsuite -y) || echo "Warning: Cannot install hhsuite without conda/mamba"'
    source: Tool
    url: https://github.com/Biomolecular-Design-Nexus/plmc_mcp
  protein-sol_mcp:
    dependencies: []
    description: '> Comprehensive protein solubility prediction and sequence analysis
      through Model Context Protocol'
    env_vars: {}
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/protein_sol_mcp
    runtime: python
    server_args:
    - src/server.py
    server_command: python
    setup_commands:
    - '(command -v mamba >/dev/null 2>&1 && mamba create -p ./env python=3.10 pandas
      -y) || (command -v conda >/dev/null 2>&1 && conda create -p ./env python=3.10
      pandas -y) || (echo "Warning: Neither mamba nor conda found, creating venv instead"
      && python3 -m venv ./env && ./env/bin/pip install pandas)'
    - ./env/bin/pip install fastmcp loguru
    - ./env/bin/pip install --ignore-installed fastmcp
    source: Tool
    url: ''
  prottrans_mcp:
    dependencies: []
    description: Protein fitness modeling using ProtTrans transformer models for embeddings
      and log-likelihood calculations
    env_vars: {}
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/prottrans_mcp
    runtime: python
    server_args:
    - src/prottrans_mcp.py
    server_command: python
    setup_commands:
    - '(command -v mamba >/dev/null 2>&1 && mamba env create -p ./env python=3.10
      -y) || (command -v conda >/dev/null 2>&1 && conda env create -p ./env python=3.10
      -y) || (echo "Warning: Neither mamba nor conda found, creating venv instead"
      && python3 -m venv ./env)'
    - ./env/bin/pip install torch==2.6.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
    - ./env/bin/pip install transformers sentencepiece
    - ./env/bin/pip install pandas loguru scikit-learn xgboost biopython sniffio
    - ./env/bin/pip install --ignore-installed fastmcp
    source: Tool
    url: https://github.com/Biomolecular-Design-Nexus/prottrans_mcp
  scripts_mcp:
    dependencies: []
    description: '> Model Context Protocol (MCP) server for protein binder design
      using BindCraft'
    env_vars: {}
    path: /home/xux/Desktop/ProteinMCP/ProteinMCP/tool-mcps/bindcraft_mcp
    runtime: python
    server_args:
    - src/server.py
    server_command: python
    setup_commands:
    - '(command -v mamba >/dev/null 2>&1 && mamba create -p ./env python=3.10 -y)
      || (command -v conda >/dev/null 2>&1 && conda create -p ./env python=3.10 -y)
      || (echo "Warning: Neither mamba nor conda found, creating venv instead" &&
      python3 -m venv ./env)'
    - ./env/bin/pip install numpy pandas biopython click
    - ./env/bin/pip install jax[cuda12] jaxlib
    - ./env/bin/pip install fastmcp loguru
    - ./env/bin/pip install --ignore-installed fastmcp
    source: Tool
    url: ''
